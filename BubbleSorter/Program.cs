namespace BubbleSorter;

class Program
{
    static void Main(string[] args)
    {
        int[] arr = { 3, 1, 4, 2 };

        // 1) Chiama OrdinaBubble(arr, true/false)
        // 2) Stampa l'array ordinato
    }

    static int[] OrdinaBubble(int[] arr, bool desc)
    {
        // TODO (idea):
        // - doppio ciclo
        // - confronta arr[j] con arr[j+1]
        // - se sono nel verso sbagliato => swap
        // - a fine return arr
        return arr;
    }
}
