namespace ArrayStats;

class Program
{
    static void Main(string[] args)
    {
        int[] numeri = { 7, 2, 9, 4, 9 };

        // 1) Chiama TrovaMax(numeri)
        // 2) Chiama ContaOccorrenze(numeri, 9)
        // 3) Stampa risultati
    }

    static int TrovaMax(int[] arr)
    {
        // TODO:
        // - parti da arr[0] come max
        // - for dal secondo elemento
        // - se trovi più grande aggiorna max
        return 0;
    }

    static int ContaOccorrenze(int[] arr, int valore)
    {
        // TODO:
        // - conta quante volte arr[i] == valore
        return 0;
    }
}
